/** Sin imports de servidor (next/headers) a propósito: la usa tanto el
 * cliente (periodo-selector.tsx, para escribir la cookie) como el
 * servidor (periodo-cookie.ts, para leerla). */
export const PERIODO_COOKIE = 'jab_periodo';
